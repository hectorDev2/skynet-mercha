import React from 'react'
import { LoadingComponent } from './components/containers/Loading'
export default function Loading () {
  // You can add any UI inside Loading, including a Skeleton.
  return <LoadingComponent />
}
